// src/db/schema.ts
// Drizzle ORM schema definitions for SEH Hub
// Tables will be added incrementally as features are built.

// This file is intentionally minimal for the bootstrap phase.
// Schema tables (users, students, parents, teachers, etc.) will be defined here.

export {}; // Ensures this is treated as a module by TypeScript
